import { extendTheme } from '@chakra-ui/react';

export const theme = extendTheme({
  config: {
    initialColorMode: "dark"
  }
  // styles: {
  //   global: {
  //     'html, body': {
  //       color: 'white',
  //       background: 'black',
  //     },
  //   },
  // },
});
